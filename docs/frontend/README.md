# Chalo Frontend Documentation

## Included

- `frontend-architecture.md` — frontend architecture, state, realtime, performance and project structure
- `ui-design-system.md` — prototype-derived visual language, colors, typography, cards, maps and components
- `user-flow.md` — passenger app flow from splash to route discovery, live trip and review
- `frontend-master-prompt.md` — complete prompt for an AI frontend engineer/agent

## Reference

Prototype repository:
https://github.com/R-V-2003/Chalo

The supplied prototype image is the visual reference. The AI agent should inspect the GitHub repository before replacing existing code.

## Important

The GitHub URL could not be reliably fetched in this environment, so the documentation does not claim specific implementation details from that repository. The master prompt explicitly instructs the coding AI to inspect it first.
